/** 菜单类型（目录） */
export const TYPE_DIR = 'M';

/** 菜单类型（菜单） */
export const TYPE_MENU = 'C';

/** 菜单类型（按钮） */
export const TYPE_BUTTON = 'F';

/** 是否菜单外链（是） */
export const IS_FRAME = 0;

/** 是否菜单外链（否） */
export const NOT_FRAME = 1;

/** 是否展示（是） */
export const IS_VISIBLE = 0;

/** 是否展示（否） */
export const NOT_VISIBLE = 1;

/** 是否缓存（是） */
export const IS_CACHE = 0;

/** 是否缓存（否） */
export const NOT_CACHE = 1;

/** 是否删除（是） */
export const IS_DELETE = '2'

/** 是否删除（否） */
export const NOT_DELETE = '0'